%li
	= link_to (image_tag user.avatar.url(:thumb), class: "img-rounded"), user
	= link_to (user.email.split("@")[0].html_safe), user
	- if current_user.admin? && current_user != user
		| #{link_to "delete", user, method: :delete, data: { confirm: "You sure?" }}